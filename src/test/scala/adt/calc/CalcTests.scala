package adt
package calc

class CalcTests extends munit.FunSuite {
  test("Basic arithmetic".ignore) {
    // assertEquals(Interpreter.interpret(Calc.literal(5.0) + Calc.literal(3.0)), 8.0)
    // assertEquals(Interpreter.interpret(Calc.literal(5.0) - Calc.literal(3.0)), 2.0)
    // assertEquals(Interpreter.interpret(Calc.literal(5.0) * Calc.literal(3.0)), 15.0)
    // assertEquals(Interpreter.interpret(Calc.literal(5.0) / Calc.literal(3.0)), 5.0 / 3.0)
  }

  test("sqrt".ignore) {
    // assertEquals(Interpreter.interpret(Calc.literal(4.0).sqrt), 2.0)
    // assertEquals(Interpreter.interpret(Calc.literal(9.0).sqrt), 3.0)
  }
}
