package adt
package shape

class ShapeTests extends munit.FunSuite {
  test("area".ignore) {
    val red = Color(1.0, 0.0, 0.0)

    // Uncomment the tests below to activate
    //
    // assertEquals(Shape.area(Circle(10.0, red)), math.Pi * 2 * 10.0)
    // assertEquals(Shape.area(Rect(3.0, 5.0, red)), 3.0 * 5.0)
  }
}
